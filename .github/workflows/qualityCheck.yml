on: [push]

jobs:
  qualityCheck:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Quality check
        run: deno task qualityCheck
